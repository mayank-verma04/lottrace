meta {
  name: Get User
  type: http
  seq: 3
}
get {
  url: {{baseUrl}}/users/:userId
  body: none
  auth: bearer
}
auth:bearer {
  token: {{accessToken}}
}
vars:pre-request {
  userId: user_id_here
}
