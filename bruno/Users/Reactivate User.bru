meta {
  name: Reactivate User
  type: http
  seq: 6
}
post {
  url: {{baseUrl}}/users/:userId/reactivate
  body: none
  auth: bearer
}
auth:bearer {
  token: {{accessToken}}
}
vars:pre-request {
  userId: user_id_here
}
