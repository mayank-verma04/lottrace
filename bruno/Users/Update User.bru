meta {
  name: Update User
  type: http
  seq: 4
}
patch {
  url: {{baseUrl}}/users/:userId
  body: json
  auth: bearer
}
auth:bearer {
  token: {{accessToken}}
}
vars:pre-request {
  userId: user_id_here
}
body:json {
  {
    "firstName": "Updated"
  }
}
