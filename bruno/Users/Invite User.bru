meta {
  name: Invite User
  type: http
  seq: 2
}
post {
  url: {{baseUrl}}/users/invite
  body: json
  auth: bearer
}
auth:bearer {
  token: {{accessToken}}
}
body:json {
  {
    "email": "newuser@example.com",
    "firstName": "New",
    "lastName": "User",
    "role": "operator"
  }
}
